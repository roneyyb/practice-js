const arr = [1, 2, 3, 4];
const arr1 = new Array(100);

const arr2 = new Array(1, 2);

arr.slice(0, 2);
array.slice(startIndex, endIndex);
//Where a copy of the array is returned from the startIndex to endIndex, both indices are optional.
//array.splice(startIndex, removeCount, item1, item2, .., itemN)
//The array is populated with all items passed as arguments (item1 to itemN) at startIndex
// after the removal of removeCount number of elements from the startIndex index.

//The concat() method merges two arrays and returns a copy of the merged arrays. The method does not modify either array passed as arguments or the array of the object’s method.
